{
  "name": "paid-blog",
  "version": "1.0.0",
  "description": "Custom Express app that uses Ghost programmatically (for middleware, payments, etc.)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
